declare module 'expo-router' {
  // Minimal typing to satisfy TS in this repo; prefer installing @types/expo__router if available
  export function useLocalSearchParams(): any;
  export function useRouter(): any;
}
